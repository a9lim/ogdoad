[package]
# The workspace language is not published as a separate crate.
name = "grundy"
version = "0.3.6"
edition = "2021"
publish = false
description = "The grundy expression language over ogdoad's scalar, Clifford, and game worlds."
license = "AGPL-3.0-or-later"
authors = ["a9lim <mx@a9l.im>"]

[dependencies]
ogdoad = { path = ".." }
